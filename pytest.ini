[pytest]
markers =
    hardware: tests requiring BLE/ANT hardware (opt-in via `pytest -m hardware`)
    release_serial: wall-clock perf assertions that must run serially
addopts = -m "not hardware" --tb=short -q
testpaths = tests
