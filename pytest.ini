[pytest]
testpaths = tests
# UI test needs playwright (browser); skip it in the default fast suite.
# Run it explicitly: pytest tests/test_dashboard_layout_ui.py -k ui
addopts = --ignore=tests/test_dashboard_layout_ui.py
python_files = test_*.py
